extend schema
@link(url: "https://specs.apollo.dev/federation/v2.0",
    import: ["@key", "@shareable"])

type Query {
    products(filteredId : [Int]): [Product]
}

type Product @key(fields: "id") {
    id: ID
    title: String
    stockQuantity: Int
}
